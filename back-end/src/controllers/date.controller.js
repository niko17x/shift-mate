import asyncHandler from "express-async-handler";
import Date from "../models/date.model.js";

// Get => 7 days respective to given week number
export const daysForWeekNum = asyncHandler(async (req, res) => {});
